swagger: "2.0"
info:
  version: "1.0"
  title: PMM-API
host: young-wave-26125.herokuapp.com
basePath: "/"
schemes:
  - http
  - https
consumes:
  - application/json
produces:
  - application/json
paths:
  /drivers:
    get:
      x-swagger-router-controller: partyResource
      parameters: []
      responses:
        '200':
          description: Definition generated from Swagger Inspector
          schema:
            $ref: '#/definitions/Drivers'
  /travels:
    post:
      parameters:
        - in: body
          name: body
          required: false
          schema:
            $ref: '#/definitions/GeographicCoordenate'
          x-examples:
            application/json: "{\n\t\"longitude\": \"33.4\",\n\t\"latitude\":\"55.5\"\n}"
      responses:
        '200':
          description: Definition generated from Swagger Inspector
          schema:
            $ref: '#/definitions/Driver'
definitions:
  Driver:
    properties:
      firstName:
        type: string
      lastName:
        type: string
      license:
        type: string
  Drivers:
    type: array
    items:
      $ref: '#/definitions/Driver'
  GeographicCoordenate:
    properties:
      longitude:
        type: string
      latitude:
        type: string